### F1: Was für Informationen werden in der Spalte "SPD" gespeichert?
In der Spalte ``SPD`` werden die Anzahl der Wahlen des dementsprehcenden Wahlbezirk für SPD gespeichert.

### F2: Was ist die höchste Bezirksnummer, die ein Wahlbezirk hat?
Die höchste Bezirsknummer ist 12.

### F3: Wie viele Wahlberechtigte gab es für den Wahlbezirk mit der Adresse "01W100"?
Es waren 819

### F4: Nennen Sie alle verschiedenen Datenwerte, die in der Spalte "Stimmart" vorkommen.
Alle Datenwerte in der SPalte ``Stimmenart`` lauten ``Erststimme``.

### F5: Welche Argumente müssen read_csv() übergeben werden? Nennen Sie keine optionalen Argumente. Der definitive Ort, um Konzepte, Methoden oder Eigenschaften von Pandas zu verstehen, ist die (umfangreiche) Dokumentation von Pandas: Dokumentation zu read_csv()
Die Methode ``read_csv()`` benötigt genau einen Parameter, welches den Dateipfad der csv-Datei bereitstellt. Alles restlichen Parameter sind optional oder haben eien Standardwert(default).

### F6: Lassen Sie sich die Variable erststimmen_datensatz ausgeben. Wie viele Zeilen und Spalten hat das Objekt?
Das Objekt hat 41 Spalten und 3598 Zeilen.

### F7: Ist erststimmen_datensatz ein DataFrame oder eine Series? Wieso?
Die Variable ``erststimmen_datensatz`` ist ein ``DataFrame`` weil es eine 2-Dimensionale Tabelle ist und nicht eine 1-Dimennsionale Darstellung.