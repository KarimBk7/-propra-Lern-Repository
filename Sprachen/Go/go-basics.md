### F1: Wo kann eine Variable mit := initialisiert werden?
Diese Variable kann inerhalb einer Funktione initialisiert werden.

### F2: Mit diesem Hintergrundwissen: Was ist der Fehler im folgenden Codeabschnitt?
Wir weisen ``x`` zu ``answer``, obwohl beide nicht den selben Typen haben. 